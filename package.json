{
  "name": "mirrortrip-website",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "verify:config": "node scripts/verify-config.js"
  },
  "engines": {
    "node": ">=20"
  }
}
