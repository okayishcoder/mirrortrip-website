{
  "name": "mirrortrip-website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx wrangler pages dev public",
    "test": "node --test",
    "verify:config": "node scripts/verify-config.js"
  },
  "engines": {
    "node": ">=20"
  }
}
